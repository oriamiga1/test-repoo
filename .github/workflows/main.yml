on:
  issue_comment:
    types: [created]

jobs:
  echo-chamber:
    if: ${{ startsWith(github.event.comment.body, '/bench ') }}
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo 'AAAAAAAAAAA'
